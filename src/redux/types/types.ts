export const types: Types = {
    fetchAll: 'FETCH_ALL',
    create: 'CREATE',
    update: 'UPDATE',
    toDelete: 'DELETE',
}

interface Types {
    fetchAll: string,
    create: string,
    update: string,
    toDelete: string,
}
