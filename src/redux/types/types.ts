export const types: Types = {
    increase: 'INCREASE',
    fetchAll: 'FETCH_ALL',
    fetchOne: 'FETCH_ONE',
    create: 'CREATE',
    update: 'UPDATE',
    toDelete: 'DELETE',
}

interface Types {
    increase: string,
    fetchAll: string,
    fetchOne: string,
    create: string,
    update: string,
    toDelete: string,
}
