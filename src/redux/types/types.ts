export const types: Types = {
    increase: 'INCREASE',
    fetchAll: 'FETCH_ALL',
    create: 'CREATE',
    update: 'UPDATE',
    toDelete: 'DELETE',
}

interface Types {
    increase: string,
    fetchAll: string,
    create: string,
    update: string,
    toDelete: string,
}
